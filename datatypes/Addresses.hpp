#pragma once
#include <cstdint>

/** Typedefs for numbers representing addresses. */

/** Data memory address. */
typedef uint8_t data_address_t;

/** Program memory address. */
typedef uint16_t program_address_t;